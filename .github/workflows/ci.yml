name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      # No lockfile and zero runtime deps — `npm ci` has nothing to install.
      - name: Test
        run: npm test

      - name: Validate skills
        run: npm run validate
